# Dependencies pinned on 2026-04-14 — Python 3.13.7
# To update: pip install -U <package> then re-run pip freeze > requirements.txt

# ── Direct dependencies ────────────────────────────────────────────────────────

# FastAPI — the web framework that runs the webhook server
fastapi==0.135.1

# Uvicorn — the ASGI server that runs FastAPI (standard extras: uvloop, websockets, watchfiles)
uvicorn==0.41.0

# asyncpg — async PostgreSQL driver for connecting to Neon
asyncpg==0.31.0

# openai — the official OpenAI Python client
openai==2.26.0

# httpx — async HTTP client for sending WhatsApp messages via Meta API
httpx==0.28.1

# python-dotenv — reads .env file and loads it into the environment
python-dotenv==1.2.2

# ── Transitive dependencies (pinned to lock the full dependency tree) ──────────

annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.12.1
certifi==2026.2.25
click==8.3.1
distro==1.9.0
h11==0.16.0
httpcore==1.0.9
httptools==0.7.1
idna==3.11
jiter==0.13.0
pydantic==2.12.5
pydantic_core==2.41.5
PyYAML==6.0.3
sniffio==1.3.1
starlette==0.52.1
tqdm==4.67.3
typing-inspection==0.4.2
typing_extensions==4.15.0
uvloop==0.22.1
watchfiles==1.1.1
websockets==16.0

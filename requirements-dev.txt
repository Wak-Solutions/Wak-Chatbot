# Test dependencies — install with: pip install -r requirements-dev.txt
# These are NOT included in requirements.txt (production) intentionally.
pytest==8.3.5
pytest-asyncio==0.25.3
pytest-mock==3.14.1
httpx==0.28.1
